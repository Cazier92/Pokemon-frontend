// npm modules
import { NavLink } from 'react-router-dom'

// types
import { User } from '../../types/models'

interface FaintScreenProps {

}

const FaintScreen = (props: FaintScreenProps): JSX.Element => {
  const {  } = props
  
  return (
    <div>
      <h1>This is a faint screen!</h1>
    </div>
  )
}

export default FaintScreen